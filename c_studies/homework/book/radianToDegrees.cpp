#include <string>
#include <iostream>
#include <cmath>

using namespace std;

string radiansToDegress(double radians) {
    double ratio = 57.29578;
    int degrees = radians / ratio;
    if (degrees > 360);
    
}